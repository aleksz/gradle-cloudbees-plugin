apply plugin: 'groovy'

dependencies {
	compile(
		'com.cloudbees:cloudbees-api-client:1.2.0',
		gradleApi()
	)
	
	groovy localGroovy()
}

uploadArchives {
	repositories {
	   flatDir {
		   dirs 'repos'
	   }
	}
}